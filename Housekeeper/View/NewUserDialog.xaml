<Window x:Class="Housekeeper.View.NewUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Icon="../Resources/Housekeeper.ico"
        Title="New User" Height="135" Width="300">
    <Grid>
        <StackPanel Margin="5,5,5,5">
            <TextBlock Text="What is your name?"/>
            <TextBox x:Name="UsernameText"/>
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Margin="0,0,5,0" Padding="8" Content="OK" 
                        Click="BtnOk_Click" />
                <Button Grid.Column="1" Margin="5,0,0,0" Padding="8" Content="Cancel" 
                        IsCancel="True"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
